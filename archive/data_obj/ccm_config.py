# CCM pairing configuration
# attributes: E, tau, train_ind_i, knn, Tp, sample, weighted, palette, col_var_obj, target_var_obj, surr_vars,
# methods: get_surr_vars()
# traits: target_var, col_var, col_var_id, target_var_id


# Experiment pairing infrastructure (slimmed from config)
# attributes: file_structure, directory_names, proj_names